# Aios Sentinel 設定範本。複製為 .env 後依需要填寫（.env 已在 .gitignore 內）。
# 所有項目都是可選的——不填也能對正式站跑完整的未認證掃描。

# 受測站台。預設為 ai_os 的 capacitor.config.ts 與 tauri.conf.json 寫死的正式站。
AIOS_TARGET=https://ai-os-app.zeabur.app

# ai_os 原始碼路徑，殼層設定稽核用（App／桌面的設定編譯進安裝檔，線上掃不到）。
AIOS_REPO=../ai_os

# 個別覆寫某一端連的站。三端正常應指同一處；發版期間 App 或桌面可能被釘在舊版，
# 那正是 build-drift 檢查要抓的東西。
# AIOS_WEB_TARGET=https://ai-os-app.zeabur.app
# AIOS_APP_TARGET=https://staging.example.com
# AIOS_DESKTOP_TARGET=https://staging.example.com

# 頁面測試的登入帳號。不提供時只巡覽公開路由，受保護頁面僅驗「有沒有正確擋下」。
# 請用權限最小的專用測試帳號，不要用管理員帳號。
# TEST_EMAIL=qa@example.com
# TEST_PW=

# 指向映像檔裡既有的 Chromium 執行檔。CI 映像常已預裝但版本編號與 playwright
# 期望的不同，設定此項可略過重新下載。
# SENTINEL_CHROMIUM_PATH=/opt/pw-browsers/chromium

# ── monitor 深度監測（全部選配）──────────────────────────────────────────
# 不填也能跑靜態＋線上段（PostHog 用戶端設定、正式站是否載入、平台 5xx、
# DB 就緒取樣、裝置紀錄）。缺金鑰時深度 API 段會在報告 facts 標略過，
# 不假裝有拉到資料。

# PostHog：拉近 24h 事件量／前端例外／裝置分布。
# POSTHOG_API_KEY=
# POSTHOG_PROJECT_ID=
# POSTHOG_HOST=https://us.posthog.com

# Zeabur：拉近期部署狀態。
# ZEABUR_API_TOKEN=
# ZEABUR_SERVICE_ID=
